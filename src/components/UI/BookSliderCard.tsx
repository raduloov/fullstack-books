import React from 'react';

interface Props {
  name: string;
  imageUrl: string;
  author: string;
  category: string;
}

const BookSliderCard = ({ name, imageUrl, author, category }: Props) => {
  return <div></div>;
};

export default BookSliderCard;
